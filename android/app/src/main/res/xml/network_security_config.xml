<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTP while testing on local LAN. Prefer HTTPS in production. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
